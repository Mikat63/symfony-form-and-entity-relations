<button type="submit" class="{{class}} px-5 py-2 {{bg|default('add-track-btn bg-white')}} text-white text-[20px] text-center rounded-lg hover:scale-105 focus:scale-105 outline-none">{{title}}</button>
